# Copy to .env and fill in. Local Postgres with trust auth (no password) is the
# default for this project (see sql/schema.sql) — leave DB_PASSWORD blank if so.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=airport_delays
DB_USER=your_macos_username
DB_PASSWORD=
